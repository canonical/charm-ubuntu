name: Spread Integration Tests
on: [pull_request]

permissions: {}

jobs:
  build:
    name: Build charm
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7
        with:
          persist-credentials: false

      - name: Set up runner
        run: |
          sudo snap install --classic concierge
          sudo snap install --classic charmcraft
          sudo concierge prepare --trace -p machine

      - name: Build charm
        run: charmcraft pack

      - name: Upload charm artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: charm
          path: ubuntu_*.charm
          if-no-files-found: error

  spread:
    name: base ${{ matrix.base }} / juju ${{ matrix.juju }}
    needs: build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        base: ["20.04", "22.04", "24.04", "26.04"]
        juju: ["3.6", "4.0"]
        exclude:
          # juju 4 no longer supports 20.04.
          - base: "20.04"
            juju: "4.0"
    steps:
      - name: Check out code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7
        with:
          persist-credentials: false

      - name: Download charm artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: charm

      - name: Set up runner
        run: |
          sudo snap install --classic concierge
          sudo snap install --classic charmcraft
          sudo concierge prepare --trace -p machine

      - name: Run spread
        env:
          CI: "true"
        run: |
          juju_slug="${{ matrix.juju }}"
          juju_slug="${juju_slug//./_}"
          charmcraft.spread -v "lxd:ubuntu-24.04:tests/spread/integration/ubuntu-${{ matrix.base }}:juju_${juju_slug}"

      - name: Tear down on failure
        if: failure()
        run: sudo concierge restore --trace
